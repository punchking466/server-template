tags:
  - 공지사항 관련(게시판)
summary: 공지사항 리스트 호출
parameters:
  - in: query
    name: curPage
    required: false
    description: offset
  - in: query
    name: pageSize
    required: false
    description: limit
  - in: query
    name: orderCol
    required: false
    description: order by
  - in: query
    name: orderVal
    required: false
    description: desc or asc
  - in: query
    name: filterCol
    required: false
    description: 조건
  - in: query
    name: filterVal
    required: false
    description: 조건
responses:
  {}
security:
  - accessToken: []